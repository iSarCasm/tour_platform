<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <%= wicked_pdf_stylesheet_link_tag "bootstrap4.min.css" -%>
  <style>
    thead { display: table-header-group }
    tfoot { display: table-row-group }
    tr { page-break-inside: avoid }
  </style>
  <title>Interim Rooming List</title>
</head>
<body>
  <p>
    <small>
      <em>Leisuretime, South Gate House, Wood Street, Cardiff, CF10 1EW</em>
    </small>
  </p>
  <div class="text-center">
    <h2 class="mb-0"><em><%= hotel_title %> - Interim Rooming List</em></h1>
    <p class="mb-1" style="color:red;"><small>Including Provisional Passengers</small></p>
    <p>
      <strong><em>
        Arriving <%= arrival_date %> for <%= nights %> nights <%= board_basis %> - departing <%= departure_date %>
      </em></strong>
    </p>
  </div>
  <p>Tour : <%= tour_ref %></p>

  <% bookings_grouped_by_room_type.each do |bookings_group| %>
    <p>
      <strong><u>Room : <%= bookings_group[:type] %></u></strong>
    </p>
    <table class="table table-sm">
      <thead>
        <tr>
          <th scope="col">Status</th>
          <th scope="col">Passenger name</th>
          <th scope="col">Booking ref</th>
          <th scope="col">Type</th>
          <th scope="col">Requests</th>
        </tr>
      </thead>
      <tbody>
        <% bookings_group[:bookings].each do |booking| %>
          <div class="alwaysbreak"></div>
          <tr>
            <td scope="col"><%= booking.status %></td>
            <td><%= booking.username %></td>
            <td><%= booking.ref %></td>
            <td><%= booking.type %></td>
            <td><%= booking.requests %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% end %>
</body>
</html>

%h1.mt-5 Hi, we are the Tour Platform
%p Check out the list of out most relevant new tours:
.container
  .row
    - @tours.each_with_index do |tour, index|
      .col
        .card{:style => "width: 20rem;"}
          = image_tag('tour_preview_1.jpg', class: 'card-img-top', alt: 'Tour Preview')
          .card-body
            %h4.card-title= tour.title
            %p.card-text= tour.description
            %a.btn.btn-primary{:href => "#"} Start the Adventure

      - if index % 3 == 2
        .w-100.mt-4

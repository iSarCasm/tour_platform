-# DO NOT REMOVE THAT LINE
- config_template(fields, options)

-# For field render you can use compact_render_field(field) or field.render
.clearfix
  - for_field :title do |field, css, id, label|
    %div{class: "col-md-3 #{css}", id: id}
      %p
        = label
      = compact_render_field field

  - for_field :rating do |field, css, id, label|
    %div{class: "col-md-3 #{css}", id: id}
      %p
        = label
      = compact_render_field field

  - for_field :phone_number do |field, css, id, label|
    %div{class: "col-md-3 #{css}", id: id}
      %p
        = label
      = compact_render_field field

  - for_field :fax_number do |field, css, id, label|
    %div{class: "col-md-3 #{css}", id: id}
      %p
        = label
      = compact_render_field field

.clearfix
  - for_field :website do |field, css, id, label|
    %div{class: "col-md-3 #{css}", id: id}
      %p
        = label
      = compact_render_field field

  - for_field :email do |field, css, id, label|
    %div{class: "col-md-3 #{css}", id: id}
      %p
        = label
      = compact_render_field field

  - for_field :contact_name do |field, css, id, label|
    %div{class: "col-md-3 #{css}", id: id}
      %p
        = label
      = compact_render_field field

  - for_field :emergency_number do |field, css, id, label|
    %div{class: "col-md-3 #{css}", id: id}
      %p
        = label
      = compact_render_field field

.clearfix
  - for_field :description do |field, css, id, label|
    %p
      = label
    = compact_render_field field

.clearfix
  - for_field :address do |field, css, id, label|
    %div{class: "col-md-3 #{css}", id: id}
      %p
        = label
      = compact_render_field field
